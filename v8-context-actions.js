(()=>{
const AR={'٠':'0','١':'1','٢':'2','٣':'3','٤':'4','٥':'5','٦':'6','٧':'7','٨':'8','٩':'9','۰':'0','۱':'1','۲':'2','۳':'3','۴':'4','۵':'5','۶':'6','۷':'7','۸':'8','۹':'9'};
const normalize=s=>String(s||'').trim().toLowerCase().replace(/[٠-٩۰-۹]/g,c=>AR[c]||c).replace(/[إأآ]/g,'ا').replace(/ى/g,'ي').replace(/ؤ/g,'و').replace(/ئ/g,'ي').replace(/ـ/g,'');
const MONTHS={يناير:1,فبراير:2,مارس:3,ابريل:4,نيسان:4,مايو:5,يونيو:6,يوليو:7,اغسطس:8,سبتمبر:9,ايلول:9,اكتوبر:10,نوفمبر:11,ديسمبر:12,'كانون الثاني':1,شباط:2,اذار:3,ايار:5,حزيران:6,تموز:7,اب:8,'تشرين الاول':10,'تشرين الثاني':11,'كانون الاول':12,january:1,february:2,march:3,april:4,may:5,june:6,july:7,august:8,september:9,october:10,november:11,december:12};
function isDeleteIntent(t){return /(الغي|الغاء|احذف|امسح|تراجع|شيل|ازل|delete|remove|undo)/i.test(t)}
function addChat(user,assistant){d.aiHistory.push({role:'user',content:user},{role:'assistant',content:assistant});$('prompt').value='';save();renderChat()}
function entryLabel(x){return `${x.type==='income'?'دخل':'مصروف'} ${money(x.amount)} بتاريخ ${x.date}${x.source?' • '+x.source:''}`}
function parseDateIntent(t){if(/اليوم|today/.test(t))return day();const n=new Date(),year=n.getFullYear();let m=t.match(/(?:يوم\s*)?(\d{1,2})\s+(يناير|فبراير|مارس|ابريل|نيسان|مايو|يونيو|يوليو|اغسطس|سبتمبر|ايلول|اكتوبر|نوفمبر|ديسمبر|شباط|اذار|ايار|حزيران|تموز|اب|january|february|march|april|may|june|july|august|september|october|november|december)/i);if(m){const mm=MONTHS[m[2].toLowerCase()],dd=+m[1],dt=new Date(year,mm-1,dd);if(dt.getMonth()===mm-1&&dt.getDate()===dd)return day(dt)}m=t.match(/(?:يوم\s*)?(\d{1,2})[\/-](\d{1,2})(?:[\/-](\d{2,4}))?/);if(m){const dd=+m[1],mm=+m[2],yy=m[3]?+(m[3].length===2?'20'+m[3]:m[3]):year,dt=new Date(yy,mm-1,dd);if(dt.getFullYear()===yy&&dt.getMonth()===mm-1&&dt.getDate()===dd)return day(dt)}return null}
function latest(list){return [...list].sort((a,b)=>String(b.date).localeCompare(String(a.date))||(+b.id||0)-(+a.id||0))[0]||null}
function handle(){const raw=$('prompt')?.value?.trim();if(!raw)return false;const t=normalize(raw);if(!isDeleteIntent(t))return false;
if(pending.length&&/(الغي|الغاء|تراجع|cancel)/i.test(t)&&!/(دخل|مصروف|بنزين|وقود|اكل|وجبه|سجل|عمليه|بيانات)/i.test(t)){pending=[];addChat(raw,'تم إلغاء الاقتراح الحالي. لم أحذف أي بيانات محفوظة.');return true}
const wantedDate=parseDateIntent(t),wantsToday=/(اليوم|today)/i.test(t),wantsIncome=/(دخل|ربح|income)/i.test(t),wantsExpense=/(مصروف|مصاريف|صرف|expense)/i.test(t),wantsFuel=/(بنزين|وقود|gas|fuel)/i.test(t),wantsFood=/(اكل|طعام|وجبه|food|meal)/i.test(t),wantsLatest=/(اخر|آخر|الاخير|الأخير|latest|last)/i.test(t);
const amountMatch=t.match(/(?:\$\s*)?(\d+(?:\.\d+)?)\s*(?:\$|دولار|دولارات|usd)?/i),wantedAmount=amountMatch?+amountMatch[1]:null;
let base=d.entries.filter(x=>(!wantsIncome||x.type==='income')&&(!wantsExpense||x.type==='expense')&&(!wantsFuel||/بنزين|وقود|gas|fuel/i.test(String(x.source||'')))&&(!wantsFood||/اكل|أكل|طعام|وجبه|وجبة|food|meal/i.test(String(x.source||'')))&&(wantedAmount===null||Math.abs((+x.amount||0)-wantedAmount)<.001));
let candidates=wantedDate?base.filter(x=>x.date===wantedDate):base;
if(wantsLatest&&candidates.length)candidates=[latest(candidates)];
if(candidates.length){pending=candidates.map(x=>({type:'delete_entry',payload:{id:x.id}}));const total=candidates.reduce((s,x)=>s+(+x.amount||0),0),kind=wantsIncome?'دخل':wantsExpense?'مصروف':'حركة';addChat(raw,`وجدت ${candidates.length} ${kind} مطابقة بقيمة إجمالية ${money(total)}${wantedDate?' بتاريخ '+wantedDate:''}. لم أحذف شيئًا بعد. راجع الاقتراح ثم اضغط «تأكيد»، أو «إلغاء».`);return true}
if(wantedDate&&base.length){const x=latest(base);pending=[{type:'delete_entry',payload:{id:x.id}}];addChat(raw,`لم أجد حركة مطابقة بتاريخ ${wantedDate}. أقرب حركة مطابقة وجدتها هي: ${entryLabel(x)}. وضعتها كاقتراح فقط ولم أحذفها. إذا كانت هي المقصودة اضغط «تأكيد»، وإلا «إلغاء».`);return true}
if(/(الغيه|الغاءه|احذفه|امسحه|تراجع عنه|شيله|هذا|هذي|هذه)/i.test(t)){const x=latest(d.entries);if(x){pending=[{type:'delete_entry',payload:{id:x.id}}];addChat(raw,`طلبك يعتمد على السياق، لذلك لم أحذف مباشرة. أحدث حركة محفوظة هي ${entryLabel(x)}. وضعتها كاقتراح فقط؛ اضغط «تأكيد» إذا كانت هي المقصودة، أو «إلغاء».`);return true}}
if((wantsIncome||wantsExpense||wantsFuel||wantsFood)&&!base.length){addChat(raw,'لم أجد في السجل حركة تطابق طلبك، لذلك لم أقترح أي حذف. يمكنك تحديد النوع والمبلغ أو التاريخ، مثل: «احذف دخل 220 دولار» أو «احذف مصروف البنزين يوم 12 سبتمبر».');return true}
addChat(raw,'فهمت أنك تريد حذف أو التراجع عن شيء، لكن لم أحدد العملية بأمان. اكتب مثلًا: «احذف آخر دخل»، «احذف دخل 220 دولار»، أو «احذف مصروف البنزين يوم 12 سبتمبر». لن أحذف شيئًا دون تأكيدك.');return true}
document.addEventListener('click',e=>{if(e.target?.id==='sendBtn'&&handle()){e.preventDefault();e.stopImmediatePropagation()}},true);
document.addEventListener('keydown',e=>{if(e.target?.id==='prompt'&&e.key==='Enter'&&!e.shiftKey&&handle()){e.preventDefault();e.stopImmediatePropagation()}},true);
})();